// Bootstrap placeholder. Owned by the Gemini integration agent
// (src/app/page.tsx) per PLAN.md section 10 — replace with the
// real whiteboard shell there.
export default function Home() {
  return (
    <main>
      <h1>AI Tutor Whiteboard</h1>
      <p>Bootstrap placeholder — integration shell pending.</p>
    </main>
  );
}
